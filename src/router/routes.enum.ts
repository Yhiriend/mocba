export enum RoutesEnum {
  LOGIN = "/login",
  HOME = "/home",
  ADD_DEVICE = "/add-device",
  SETTINGS = "/settings",
  DEVICES = "/devices",
  DEVICE_DETAIL = "/devices/:id",
  ALERTS = "/alerts",
  REGISTER = "/register",
}
